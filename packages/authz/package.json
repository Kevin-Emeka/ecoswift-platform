{
  "name": "@ecoswift/authz",
  "version": "0.1.0",
  "private": true,
  "description": "Shared authorization primitives for Ecoswift Bank — permission catalog, guards, and policy evaluation reusable across every service",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ecoswift/cache": "workspace:*",
    "@ecoswift/config": "workspace:*",
    "@ecoswift/database": "workspace:*",
    "@ecoswift/shared": "workspace:*",
    "@nestjs/common": "^10.4.15",
    "@nestjs/core": "^10.4.15"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.10.2",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.7.2"
  }
}

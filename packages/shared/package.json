{
  "name": "@ecoswift/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared cross-cutting building blocks (base exceptions, DTOs, constants) used across Ecoswift Bank services",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ecoswift/types": "workspace:*",
    "@nestjs/common": "^10.4.15",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.1"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}

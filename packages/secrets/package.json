{
  "name": "@ecoswift/secrets",
  "version": "0.1.0",
  "private": true,
  "description": "Secrets manager abstraction for Ecoswift Bank (env-based for development, AWS Secrets Manager for production)",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.703.0",
    "@nestjs/common": "^10.4.15"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
